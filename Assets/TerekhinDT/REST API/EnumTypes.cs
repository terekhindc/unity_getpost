namespace TerekhinDT.REST_API
{
    public enum RequestTypes {Get, Post, Put, Delete}
    public enum ResponseTypes {Json, Image, Video, Sound}
    public enum FormDataTypes {None, FormData, UrlEncodedForm, Raw}
}